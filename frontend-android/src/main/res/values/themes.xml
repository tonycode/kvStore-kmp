<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!--region Theme.MinApp -->
    <style name="Base.V21.Light.Theme.MinApp" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="android:windowTranslucentStatus">true</item>

        <!-- Transparent system bars for edge-to-edge. -->
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:windowDrawsSystemBarBackgrounds">true</item>

        <!--<item name="android:navigationBarColor">?attr/colorPrimaryVariant</item>-->
        <!--<item name="android:statusBarColor">?attr/colorPrimaryVariant</item>-->

        <item name="android:windowBackground">@color/splashscreen_bg_light</item>
    </style>

    <style name="Base.V21.Theme.MinApp" parent="Base.V21.Light.Theme.MinApp" />

    <style name="Base.Theme.MinApp" parent="Base.V21.Theme.MinApp" />

    <style name="Theme.MinApp" parent="Base.Theme.MinApp" />
    <!--endregion-->

</resources>
